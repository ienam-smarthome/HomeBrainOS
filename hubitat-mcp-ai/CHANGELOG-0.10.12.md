# 0.10.12

- Pronoun follow-ups now use the Control Agent's verified device-ID context.
- The entire prior device group remains available to immediate `it` and `them`
  follow-ups.
- Missing context produces a deterministic no-write clarification instead of an
  AI guess.
