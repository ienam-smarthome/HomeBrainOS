# Hubitat MCP AI 0.10.249

- Lists each automation app and Rule Machine rule with an explicit **ACTIVE**, **DISABLED**, **PAUSED**, **BROKEN**, or **UNKNOWN** status.
- Uses live app and rule state rather than inferring active state from `paused=false`.
- Adds coloured status badges and matching coloured section headings in the WebUI.
- Updates the Rules shortcut to request complete per-item status coverage.
