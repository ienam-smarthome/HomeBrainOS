# Hubitat MCP AI 0.10.200

- Fix API URL resolution when Home Assistant ingress opens the panel without a
  trailing slash.
- Preserve the complete ingress token path for status, ask, and MCP refresh
  requests.
