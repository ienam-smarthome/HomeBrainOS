# Hubitat MCP AI 0.10.211

- Restores live dashboard counts by requesting the detailed `attributes` payload
  supported by the upstream Hubitat MCP device serializer.
- Renders common Markdown headings, lists, and bold text as readable UI elements.
- Removes Markdown control characters before reading answers aloud.
- Adds a compact copy control for the complete technical JSON response.
