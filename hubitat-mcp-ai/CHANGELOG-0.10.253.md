# Hubitat MCP AI 0.10.253

## Changes

- Wire the natural home-summary wording into the live deterministic snapshot presenter.
- Replace the ambiguous `Alerts` label with an explicit offline/unavailable device warning.
- Replace `Other switches on` with clearer non-light switch wording.
- Add regression coverage for the live home-summary response.
