# 0.10.15

- Device clarification cards now let users click the intended device instead of typing its number.
- Clicked choices use the same pending, session-scoped control flow as typed or spoken replies.
- Choice cards support mouse, touch, Enter and Space interaction with accessible labels and focus styling.
