# Hubitat MCP AI 0.10.65

- Adds an **Apps** smart shortcut beside **Rules** in the HomeBrain Web UI.
- The shortcut sends `List apps` to the deterministic app inventory controller.
- Adds regression coverage for shortcut placement and duplicate prevention.
