# Hubitat MCP AI 0.10.74

- Fixes `find all devices` being answered as a failed named-device lookup.
- Adds a terminal `device-inventory` response with device, room and disabled
  counts plus the complete selected Hubitat device list.
- Keeps broad inventory intent separate from targeted entity resolution.
