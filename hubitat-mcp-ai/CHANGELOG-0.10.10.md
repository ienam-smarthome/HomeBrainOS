# 0.10.10

- Plural room/type commands such as `turn on hallway lights` now control every
  matching selected light in that room without an unnecessary device chooser.
- Singular targets remain singular and ambiguous targets still request a choice.
- Exact plural device names take precedence over inferred room groups.
