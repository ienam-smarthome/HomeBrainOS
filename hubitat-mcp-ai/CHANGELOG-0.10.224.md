# Hubitat MCP AI 0.10.224

- Moves MCP tools, Ollama model, last route, and response time into the compact top status strip.
- Removes duplicate dashboard metric and shortcut controls.
- Adds room device counts and a live active-rooms summary based on motion or lights.
- Adds a structured Hub Info panel for firmware, update availability, CPU, memory, temperature, uptime, database size, IP address, and Matter status.
- Reads Hub Info values directly from the matching Hubitat device instead of relying on AI-generated summaries.
