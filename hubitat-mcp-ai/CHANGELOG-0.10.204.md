# Hubitat MCP AI 0.10.204

- Detect repeated Ollama tool calls and force a final evidence-based answer with
  tools disabled instead of exhausting the MCP round limit.
- Force a final synthesis pass when the configured round limit is reached.
- Improve long Ollama model-name wrapping in dashboard tiles.
