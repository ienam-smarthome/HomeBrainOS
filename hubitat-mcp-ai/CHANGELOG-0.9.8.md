# 0.9.8

- Publish the targeted device-lookup repair under a new Home Assistant add-on version.
- Allow installations already running 0.9.7 to discover and install the structured
  `homebrain_search_devices` resolution fix.
- Keep the add-on manifest and runtime version metadata aligned.
