# Hubitat MCP AI 0.14.11

## Compact mobile controls and runtime status

- Put **Ask** and **Speak** side by side in a 50/50 action row directly below
  the query input.
- Keep the existing query field, read-aloud option, and answer panel unchanged.
- Reduce padding, gap, and font size for the MCP/Ollama/model/runtime status
  pills so the status card consumes less vertical space on mobile.
- Preserve the query-first layout, compact System Check, targeted finding
  colours, and red **BROKEN** state from the previous releases.
- Add regression checks for the two-column query actions and compact status card.
