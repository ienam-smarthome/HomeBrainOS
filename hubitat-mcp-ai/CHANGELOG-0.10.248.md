# Hubitat MCP AI 0.10.248

- Shows an **Update hub firmware** action whenever the authoritative hub firmware response reports that a newer version is available.
- Detects structured `update_available` evidence when exposed and supports the current installed/available version wording as a safe fallback.
- Sends the existing confirmation-based firmware installation request when the action is clicked.
