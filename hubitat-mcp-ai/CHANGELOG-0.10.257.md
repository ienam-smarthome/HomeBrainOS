# Hubitat MCP AI 0.10.257

- Render automation results as collapsible status sections with live counts.
- Collapse Active and Disabled by default while expanding Paused, Broken, and Unknown.
- Add status filter buttons, name search, Expand all, and Collapse all controls.
- Label each item as an App or Rule Machine rule.
- Remove duplicate `(Paused)` and `*BROKEN*` markers from displayed names when structured status is already shown.
