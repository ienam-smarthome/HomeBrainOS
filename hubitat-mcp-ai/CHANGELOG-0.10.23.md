# 0.10.23

- Makes the Hubitat MCP AI dashboard installable as a Progressive Web App.
- Adds a web-app manifest, standalone display mode, app icon and theme metadata.
- Adds a safe shell-only service worker that never caches live `/api/` responses.
- Keeps the normal browser page available while allowing Samsung Internet or Chrome to add the dashboard as an app.