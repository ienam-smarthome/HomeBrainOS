from __future__ import annotations

import html
import json


def render_page(title: str, version: str) -> str:
    """Render the self-contained Home Assistant ingress UI."""

    safe_title = html.escape(title)
    # json.dumps() does not escape a literal "</script>" sequence inside the
    # string it encodes -- these values are interpolated directly into an
    # inline <script> block below, so an unescaped "</script>" in either
    # value would close the script tag early and let anything following it
    # be parsed as raw HTML. title comes from admin/add-on config
    # (web_title), not a remote-input surface, but the escape is cheap and
    # correct defense-in-depth regardless of where the value originates.
    # The standard fix: escape the forward slash in "</" so the JS engine
    # still parses it as the same string via a (harmless) backslash escape,
    # while the raw "</script>" byte sequence never appears in the markup.
    title_json = json.dumps(title).replace("</", "<\\/")
    version_json = json.dumps(version).replace("</", "<\\/")
    return (
        r"""<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<meta name="theme-color" content="#0b0b0c">
<title>__SAFE_TITLE__</title>
<style>
:root{color-scheme:dark;--bg:#0b0b0c;--card:#1f1f21;--tile:#303033;--text:#fff;--muted:#b9b9bd;--blue:#2f7df6;--green:#166534;--red:#991b1b;--amber:#b45309}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:Arial,sans-serif}.wrap{max-width:960px;margin:auto;padding:24px}h1{font-size:36px;margin:8px 0 20px}.card{background:var(--card);border-radius:18px;padding:16px;margin:12px 0}.row,.grid{display:flex;gap:10px;flex-wrap:wrap}.pill{padding:8px 12px;border-radius:999px;background:#3a3a3d;font-size:13px}.pill.ok{background:var(--green)}.pill.error{background:var(--red)}.pill strong{margin-left:4px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr))}.tile{background:var(--tile);border-radius:14px;padding:14px;min-width:0}.big{font-size:22px;font-weight:700;overflow-wrap:anywhere;word-break:break-word}.muted{color:var(--muted);font-size:13px}input,button{width:100%;border:0;border-radius:12px;padding:14px;margin:7px 0;font:inherit}input{background:#fff;color:#111}button{background:var(--blue);color:#fff;cursor:pointer}.secondary{background:#333}.confirm{background:var(--green)}.answer{background:#000;border-radius:12px;padding:14px;min-height:52px;margin-top:8px;white-space:pre-wrap;overflow-wrap:anywhere}.answer-meta{display:flex;gap:7px;flex-wrap:wrap;margin-bottom:10px}.badge{background:#243044;border-radius:999px;padding:4px 8px;font-size:12px}.outcome-badge{font-weight:700}.outcome-positive{background:#14532d;color:#bbf7d0}.outcome-warning{background:#78350f;color:#fde68a}.outcome-neutral{background:#3f3f46;color:#e4e4e7}.outcome-critical{background:#7f1d1d;color:#fecaca}.answer-text{white-space:pre-wrap}.technical{margin-top:12px;color:var(--muted)}.technical summary{cursor:pointer}.technical pre{white-space:pre-wrap;overflow:auto;max-height:260px;background:#111;padding:10px;border-radius:8px}.busy{opacity:.65}
.answer-text p{margin:0 0 10px}.answer-text p:last-child{margin-bottom:0}.answer-text h2,.answer-text h3{margin:14px 0 6px;font-size:18px}.answer-text ul,.answer-text ol{margin:6px 0 10px;padding-left:24px}.answer-text li{margin:5px 0}.status-label{display:inline-block;border-radius:999px;padding:2px 8px;margin-right:7px;font-size:11px;font-weight:700;letter-spacing:.03em;vertical-align:1px}.status-active{background:#14532d;color:#bbf7d0}.status-disabled{background:#3f3f46;color:#e4e4e7}.status-paused{background:#78350f;color:#fde68a}.status-broken{background:#7f1d1d;color:#fecaca}.status-unknown{background:#1e3a8a;color:#bfdbfe}.inline-critical{color:#f87171;font-weight:700}.heading-active{color:#4ade80}.heading-disabled{color:#a1a1aa}.heading-paused{color:#fbbf24}.heading-broken{color:#f87171}.heading-unknown{color:#60a5fa}
.automation-view{white-space:normal}.automation-summary{margin:0 0 10px}.automation-toolbar{display:flex;gap:7px;flex-wrap:wrap;margin:10px 0}.automation-toolbar input{flex:1 1 220px;margin:0;padding:10px}.automation-toolbar button{width:auto;margin:0;padding:9px 11px;font-size:12px}.automation-filters{display:flex;gap:6px;flex-wrap:wrap;margin:8px 0 12px}.automation-filter{width:auto;margin:0;padding:7px 10px;background:#27272a;font-size:12px}.automation-filter.selected{background:var(--blue)}.automation-section{border:1px solid #29292d;border-radius:10px;margin:8px 0;background:#111}.automation-section summary{cursor:pointer;padding:11px 12px;font-weight:700;list-style-position:inside}.automation-section ul{margin:0;padding:0 12px 10px 34px}.automation-section li{margin:6px 0}.automation-section[hidden]{display:none}.automation-kind{color:var(--muted);font-size:11px;margin-left:6px}.automation-empty{color:var(--muted);padding:8px 0}.automation-section-active summary{color:#4ade80}.automation-section-disabled summary{color:#a1a1aa}.automation-section-paused summary{color:#f87171}.automation-section-broken summary{color:#f87171}.automation-section-unknown summary{color:#60a5fa}
.mic-fab{position:fixed;right:18px;bottom:18px;z-index:10;width:64px;height:64px;margin:0;border-radius:50%;font-size:27px;background:var(--green);box-shadow:0 10px 30px rgba(0,0,0,.5)}.mic-fab.listening{background:#dc2626}.copy-button{width:auto;display:block;margin:10px 0 0 auto;padding:7px 10px;font-size:12px}.technical-copy{margin-top:7px}.copy-ok{background:var(--green)}.copy-fail{background:var(--red)}
@media(max-width:520px){.wrap{padding:12px 12px 88px}h1{font-size:28px}.card{border-radius:12px;padding:11px}.grid{grid-template-columns:repeat(2,minmax(0,1fr))}.mic-fab{right:14px;bottom:14px;width:68px;height:68px}.automation-toolbar input{flex-basis:100%}}
</style>
</head>
<body><main class="wrap">
<h1>🏠 <span id="title"></span> <small id="version"></small></h1>
<section class="card row"><span class="pill" id="mcp">MCP unknown</span><span class="pill"><span class="muted">MCP tools</span><strong id="tools">—</strong></span><span class="pill" id="ollama">Ollama unknown</span><span class="pill"><span class="muted">Model</span><strong id="model">—</strong></span><span class="pill"><span class="muted">Last route</span><strong id="lastRoute">—</strong></span><span class="pill"><span class="muted">Response</span><strong id="lastTime">—</strong></span></section>
<section class="card grid">
<button class="tile secondary" data-q="Which lights are on?"><div class="big" id="dashLights">—</div><div>Lights on</div><div class="muted">Tap for live details</div></button>
<button class="tile secondary" data-q="Which rooms are active?"><div class="big" id="dashActiveRooms">—</div><div>Active rooms</div><div class="muted" id="dashActiveRoomNames">Motion or lights on</div></button>
<button class="tile secondary" data-q="Which motion sensors are active?"><div class="big" id="dashMotion">—</div><div>Motion active</div><div class="muted">Live Hubitat states</div></button>
<button class="tile secondary" data-q="Which switches are on?"><div class="big" id="dashSwitches">—</div><div>Switches on</div><div class="muted">Excludes lights</div></button>
<button class="tile secondary" data-q="Which batteries are low?"><div class="big" id="dashBatteries">—</div><div>Low batteries</div><div class="muted">At or below 20%</div></button>
</section>
<section class="card">
<input id="query" placeholder="Ask your Hubitat…" autocomplete="off">
<button id="ask">Ask</button><button class="secondary" id="speak">🎤 Speak</button>
<label class="muted"><input id="readAnswers" type="checkbox" style="width:auto"> Read answers aloud</label>
<div class="answer" id="answer">Ready</div>
</section>
<section class="card grid" id="shortcuts">
<button class="secondary" data-q="What's happening at home?">🏠 What's happening?</button>
<button class="secondary" data-q="List every automation app and Rule Machine rule with its current active, disabled, paused, broken, or unknown status">⚙️ Rules</button>
<button class="secondary" data-q="Check the hub health status">🧠 Hub health</button>
<button class="secondary" data-q="Show hub CPU and free memory">📊 Hub resources</button>
<button class="secondary" data-q="List devices that are offline or stale">⚠️ Device health</button>
<button class="secondary" data-q="What is the weather?">🌦️ Weather</button>
<button class="secondary" data-q="Recommend useful automations for my home">✨ Recommendations</button>
<button class="secondary" id="refresh">🧰 Refresh MCP tools</button>
</section>
<p class="muted">Powered by Ollama Online native function calling and Hubitat MCP.</p>
</main>
<button id="micFab" class="mic-fab" aria-label="Speak">🎤</button>
<script>
const TITLE=__TITLE__,VERSION=__VERSION__;
const newSessionId=()=>globalThis.crypto?.randomUUID?.()||`hmcp-${Date.now().toString(36)}-${Math.random().toString(36).slice(2)}`;
const sessionId=sessionStorage.getItem('hmcp_session_id')||newSessionId();
sessionStorage.setItem('hmcp_session_id',sessionId);
document.getElementById('title').textContent=TITLE;document.getElementById('version').textContent='v'+VERSION;
const query=document.getElementById('query'),ask=document.getElementById('ask'),answer=document.getElementById('answer');
query.value=localStorage.getItem('hmcp_last_query')||'';
const apiPath=path=>`${location.pathname.replace(/\/?$/,'/')}${path}`;
function pill(id,ok,text){const node=document.getElementById(id);node.textContent=text;node.className='pill '+(ok?'ok':'error')}
async function jsonResponse(response){const raw=await response.text();try{return JSON.parse(raw)}catch(error){throw new Error(`HTTP ${response.status}: ${raw||'empty response'}`)}}
let activeSpeech=null;function stopAudio(){if('speechSynthesis'in globalThis)speechSynthesis.cancel();activeSpeech=null;document.querySelectorAll('.audio-button').forEach(button=>button.textContent='🔊 Read answer')}
function speakAnswer(value,button=null){const spoken=speechText(value);if(!spoken||!('speechSynthesis'in globalThis))return;stopAudio();const utterance=new SpeechSynthesisUtterance(spoken);utterance.lang='en-GB';utterance.rate=.95;utterance.pitch=1;const voices=speechSynthesis.getVoices();utterance.voice=voices.find(voice=>voice.lang?.toLowerCase().startsWith('en-gb')&&voice.localService)||voices.find(voice=>voice.lang?.toLowerCase().startsWith('en'))||null;activeSpeech=utterance;if(button)button.textContent='■ Stop audio';utterance.onend=utterance.onerror=()=>{if(activeSpeech===utterance){activeSpeech=null;if(button)button.textContent='🔊 Read answer'}};speechSynthesis.speak(utterance)}
let readAnswers=localStorage.getItem('hmcp_read_answers')==='true';document.getElementById('readAnswers').checked=readAnswers;document.getElementById('readAnswers').onchange=event=>{readAnswers=event.target.checked;localStorage.setItem('hmcp_read_answers',String(readAnswers));if(!readAnswers)stopAudio()};
function legacyCopy(value){const area=document.createElement('textarea');area.value=value;area.setAttribute('readonly','');area.style.position='fixed';area.style.opacity='0';document.body.appendChild(area);area.focus();area.select();area.setSelectionRange(0,area.value.length);let ok=false;try{ok=Boolean(document.execCommand&&document.execCommand('copy'))}catch(error){ok=false}area.remove();return ok}
async function copyText(value,button,label){let ok=legacyCopy(value);if(!ok&&globalThis.isSecureContext&&navigator.clipboard?.writeText){try{await navigator.clipboard.writeText(value);ok=true}catch(error){ok=false}}button.textContent=ok?'Copied':'Copy blocked — select text';button.className=`copy-button ${ok?'copy-ok':'copy-fail'}`;setTimeout(()=>{button.textContent=label;button.className='secondary copy-button'+(label==='Copy technical'?' technical-copy':'')},1800)}
function appendInline(parent,value){String(value).split(/(\*\*[^*]+\*\*|\(Paused\)|\*BROKEN\*)/gi).filter(Boolean).forEach(part=>{if(part.startsWith('**')&&part.endsWith('**')){const strong=document.createElement('strong');strong.textContent=part.slice(2,-2);parent.appendChild(strong)}else if(/^\(Paused\)$/i.test(part)||/^\*BROKEN\*$/i.test(part)){const warning=document.createElement('span');warning.className='inline-critical';warning.textContent=/paused/i.test(part)?'(Paused)':'BROKEN';parent.appendChild(warning)}else parent.appendChild(document.createTextNode(part.replace(/`([^`]+)`/g,'$1')))})}
function statusName(value){const match=String(value).match(/^\[(ACTIVE|DISABLED|PAUSED|BROKEN|UNKNOWN)\]\s*/i);return match?match[1].toLowerCase():null}
function appendStatusLine(parent,value){const status=statusName(value);let text=String(value);if(status){text=text.replace(/^\[(?:ACTIVE|DISABLED|PAUSED|BROKEN|UNKNOWN)\]\s*/i,'');const label=document.createElement('span');label.className='status-label status-'+status;label.textContent=status.toUpperCase();parent.appendChild(label)}appendInline(parent,text)}
function headingStatus(value){const normalized=String(value).replace(/:$/,'').trim().toLowerCase();return ['active','disabled','paused','broken','unknown'].find(status=>normalized===status||normalized===status+' rules'||normalized===status+' apps')||null}
function renderMessage(value){const root=document.createElement('div');root.className='answer-text';let list=null;String(value).replace(/\r/g,'').split('\n').forEach(raw=>{const line=raw.trim();if(!line){list=null;return}const bullet=line.match(/^[-*]\s+(.+)$/);if(bullet){if(!list){list=document.createElement('ul');root.appendChild(list)}const item=document.createElement('li');appendStatusLine(item,bullet[1]);list.appendChild(item);return}list=null;const heading=line.match(/^#{1,3}\s+(.+)$/);const node=document.createElement(heading?'h3':'p');const headingText=heading?heading[1]:line;const state=headingStatus(headingText);if(state)node.classList.add('heading-'+state);appendStatusLine(node,headingText);root.appendChild(node)});return root}
const AUTOMATION_STATUSES=['active','disabled','paused','broken','unknown'];
function cleanAutomationName(value){return String(value||'').replace(/\s*\(Paused\)\s*/gi,' ').replace(/\s*\*BROKEN\*\s*/gi,' ').replace(/\s{2,}/g,' ').trim()}
function renderAutomationItems(data){const root=document.createElement('div');root.className='automation-view';const items=(Array.isArray(data.automation_items)?data.automation_items:[]).filter(item=>AUTOMATION_STATUSES.includes(String(item.status||'').toLowerCase()));const intro=document.createElement('p');intro.className='automation-summary';intro.textContent=`Hubitat returned ${items.length} automation items.`;root.appendChild(intro);const toolbar=document.createElement('div');toolbar.className='automation-toolbar';const search=document.createElement('input');search.type='search';search.placeholder='Search apps and rules…';search.setAttribute('aria-label','Search automation items');const expand=document.createElement('button');expand.className='secondary';expand.textContent='Expand all';const collapse=document.createElement('button');collapse.className='secondary';collapse.textContent='Collapse all';toolbar.append(search,expand,collapse);root.appendChild(toolbar);const filters=document.createElement('div');filters.className='automation-filters';let selected='all';['all',...AUTOMATION_STATUSES].forEach(status=>{const button=document.createElement('button');button.className='automation-filter'+(status==='all'?' selected':'');button.dataset.status=status;button.textContent=status==='all'?'All':status[0].toUpperCase()+status.slice(1);button.onclick=()=>{selected=status;filters.querySelectorAll('button').forEach(node=>node.classList.toggle('selected',node===button));apply()};filters.appendChild(button)});root.appendChild(filters);const sections=new Map();AUTOMATION_STATUSES.forEach(status=>{const matching=items.filter(item=>String(item.status).toLowerCase()===status);if(!matching.length)return;const details=document.createElement('details');details.className=`automation-section automation-section-${status}`;details.dataset.status=status;details.open=['paused','broken','unknown'].includes(status);const summary=document.createElement('summary');summary.textContent=`${status[0].toUpperCase()+status.slice(1)} (${matching.length})`;const list=document.createElement('ul');matching.sort((a,b)=>String(a.name).localeCompare(String(b.name))).forEach(item=>{const row=document.createElement('li');row.dataset.search=`${item.name||''} ${item.type||''}`.toLowerCase();const label=document.createElement('span');label.className=`status-label status-${status}`;label.textContent=status.toUpperCase();const name=document.createElement('span');name.textContent=cleanAutomationName(item.name);const kind=document.createElement('span');kind.className='automation-kind';kind.textContent=item.type==='rule'?'Rule Machine':'App';row.append(label,name,kind);list.appendChild(row)});details.append(summary,list);root.appendChild(details);sections.set(status,details)});const empty=document.createElement('div');empty.className='automation-empty';empty.textContent='No automation items match this filter.';empty.hidden=true;root.appendChild(empty);function apply(){const term=search.value.trim().toLowerCase();let visibleSections=0;sections.forEach((details,status)=>{let visibleRows=0;details.querySelectorAll('li').forEach(row=>{const show=(!term||row.dataset.search.includes(term))&&(selected==='all'||selected===status);row.hidden=!show;if(show)visibleRows+=1});details.hidden=visibleRows===0;if(visibleRows){visibleSections+=1;details.querySelector('summary').textContent=`${status[0].toUpperCase()+status.slice(1)} (${visibleRows})`}});empty.hidden=visibleSections!==0}search.oninput=apply;expand.onclick=()=>sections.forEach(details=>{if(!details.hidden)details.open=true});collapse.onclick=()=>sections.forEach(details=>details.open=false);return root}
function speechText(value){return String(value).replace(/```[\s\S]*?```/g,' ').replace(/\[(ACTIVE|DISABLED|PAUSED|BROKEN|UNKNOWN)\]/gi,'$1').replace(/\[([^\]]+)\]\([^)]+\)/g,'$1').replace(/\s*\((?:IDs?|device IDs?)\s*:\s*[^)]+\)/gi,'').replace(/\b(?:IDs?|device IDs?)\s*[:#]\s*[\d,\s]+/gi,'').replace(/([a-z])([A-Z])/g,'$1 $2').replace(/\bRTT\b/gi,'response time').replace(/\bCPU\b/g,'C P U').replace(/&/g,' and ').replace(/\s+\/\s+/g,' or ').replace(/:\s*\n/g,'.\n').replace(/^\s*#{1,6}\s*/gm,'').replace(/^\s*[-*+]\s+/gm,'').replace(/>/g,' over ').replace(/=/g,' ').replace(/(\d+(?:\.\d+)?)h\b/gi,'$1 hours').replace(/[*_`]/g,'').replace(/\n+/g,'. ').replace(/\s+/g,' ').replace(/\s+\./g,'.').replace(/\.{2,}/g,'.').trim()}
const historyKey=`hmcp_history_${sessionId}`;let suggestedAction=null,conversationHistory=[];try{const storedHistory=JSON.parse(sessionStorage.getItem(historyKey)||'[]');if(Array.isArray(storedHistory))conversationHistory=storedHistory.slice(-8)}catch(error){sessionStorage.removeItem(historyKey)}
function firmwareUpdateAvailable(data,rawMessage){if(data?.update_available===true)return true;const evidence=Array.isArray(data?.evidence)?data.evidence:[];if(evidence.some(item=>item?.update_available===true||item?.data?.update_available===true||item?.result?.update_available===true))return true;return /\bfirmware update is available\b/i.test(rawMessage)||/\bhub firmware\s+\S+\s+is installed and\s+\S+\s+is available\b/i.test(rawMessage)}
function choicePrompt(question,choice){const action=question.match(/\b(turn\s+on|turn\s+off|toggle)\b/i)?.[1]||'';if(action)return `${action} ${choice}`;const attribute=question.match(/\b(temperature|humidity|battery|power)\b/i)?.[1]||'';if(attribute)return `What is the ${choice} ${attribute}?`;return choice}
function showAnswer(data){answer.replaceChildren();const meta=document.createElement('div');meta.className='answer-meta';const outcome=data.outcome_presentation;if(outcome&&['positive','warning','neutral','critical'].includes(outcome.tone)&&typeof outcome.label==='string'&&outcome.label){const outcomeBadge=document.createElement('span');outcomeBadge.className=`badge outcome-badge outcome-${outcome.tone}`;outcomeBadge.textContent=outcome.label;meta.appendChild(outcomeBadge)}[data.route,data.model,data.elapsed_ms===undefined?null:`${(data.elapsed_ms/1000).toFixed(1)}s`].filter(Boolean).forEach(value=>{const badge=document.createElement('span');badge.className='badge';badge.textContent=value;meta.appendChild(badge)});if(meta.children.length)answer.appendChild(meta);const rawMessage=data.message||data.detail||'No response';const text=renderMessage(rawMessage);answer.appendChild(text);if(data.route==='automation-status'&&Array.isArray(data.automation_items)&&data.automation_items.length)answer.appendChild(renderAutomationItems(data));if(Array.isArray(data.choices)){const originalQuestion=query.value.trim();data.choices.forEach(choice=>{const option=document.createElement('button');option.className='confirm';option.textContent=choice;option.onclick=()=>submit(choicePrompt(originalQuestion,choice));answer.appendChild(option)})}suggestedAction=firmwareUpdateAvailable(data,rawMessage)?'firmware-update':null;if(suggestedAction==='firmware-update'){const install=document.createElement('button');install.className='confirm';install.textContent='Update hub firmware';install.onclick=()=>submit('Install the available Hubitat firmware update');answer.appendChild(install)}if(data.confirmation_required===true){const confirm=document.createElement('button');confirm.className='confirm';confirm.textContent='Confirm action';confirm.onclick=()=>submit('confirm');answer.appendChild(confirm)}const audioButton=document.createElement('button');audioButton.className='secondary copy-button audio-button';audioButton.textContent='🔊 Read answer';audioButton.onclick=()=>{if(activeSpeech)stopAudio();else speakAnswer(rawMessage,audioButton)};answer.appendChild(audioButton);const copy=document.createElement('button');copy.className='secondary copy-button';copy.textContent='Copy answer';copy.onclick=()=>copyText(rawMessage,copy,'Copy answer');answer.appendChild(copy);const details=document.createElement('details');details.className='technical';const summary=document.createElement('summary');summary.textContent='Technical details';const technical=JSON.stringify(data,null,2);const technicalCopy=document.createElement('button');technicalCopy.className='secondary copy-button technical-copy';technicalCopy.textContent='Copy technical';technicalCopy.onclick=event=>{event.preventDefault();copyText(technical,technicalCopy,'Copy technical')};const pre=document.createElement('pre');pre.textContent=technical;details.append(summary,technicalCopy,pre);answer.appendChild(details);document.getElementById('lastRoute').textContent=data.route||'—';document.getElementById('lastTime').textContent=data.elapsed_ms===undefined?'—':`${(data.elapsed_ms/1000).toFixed(1)}s`;if(readAnswers&&speechText(rawMessage).length<1500)speakAnswer(rawMessage,audioButton)}
async function status(){try{const data=await jsonResponse(await fetch(apiPath('api/status')));pill('mcp',data.mcp?.online,data.mcp?.online?'MCP online':`MCP offline · ${data.mcp?.error||'unavailable'}`);pill('ollama',data.ollama?.configured,data.ollama?.configured?'Ollama ready':'Ollama API key required');document.getElementById('tools').textContent=data.mcp?.tools??'—';document.getElementById('model').textContent=data.ollama?.model||'—'}catch(error){pill('mcp',false,'Status error · '+error.message)}try{const dash=await jsonResponse(await fetch(apiPath('api/dashboard')));document.getElementById('dashLights').textContent=dash.lights_on??'—';document.getElementById('dashMotion').textContent=dash.motion_active??'—';document.getElementById('dashSwitches').textContent=dash.switches_on??'—';document.getElementById('dashBatteries').textContent=dash.low_batteries??'—';const active=dash.active_rooms||[];const loaded=[dash.lights_on,dash.motion_active,dash.switches_on,dash.low_batteries].some(value=>value!=null);document.getElementById('dashActiveRooms').textContent=loaded?active.length:'—';document.getElementById('dashActiveRoomNames').textContent=loaded?(active.length?active.map(room=>room.name).join(', '):'No motion or lights on'):'Loading live states…'}catch(error){console.warn('Dashboard unavailable',error)}}
const renderAnswer=showAnswer;showAnswer=data=>{renderAnswer(data);const question=query.value.trim();if(question){const asked=document.createElement('div');asked.style.cssText='color:var(--muted);font-size:13px;margin-bottom:9px;padding-bottom:9px;border-bottom:1px solid #252525';asked.textContent='Question: '+question;answer.prepend(asked)}};
let activeRequest=null,requestSequence=0;
async function submit(text){text=(text||query.value).trim();if(!text)return;const normalized=text.toLowerCase().replace(/[.!]/g,'').trim();if(suggestedAction==='firmware-update'&&['yes','yes please','proceed','do it'].includes(normalized))text='Install the available Hubitat firmware update';suggestedAction=null;stopAudio();query.value=text;localStorage.setItem('hmcp_last_query',text);if(activeRequest)activeRequest.abort();const controller=new AbortController();activeRequest=controller;const sequence=++requestSequence;const history=conversationHistory.slice(-8);answer.classList.add('busy');answer.textContent='Working… Ask another question to replace this request.';try{const data=await jsonResponse(await fetch(apiPath('api/ask'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({query:text,history,session_id:sessionId}),signal:controller.signal}));if(sequence===requestSequence){conversationHistory.push({role:'user',content:text},{role:'assistant',content:data.message||data.detail||''});conversationHistory=conversationHistory.slice(-8);sessionStorage.setItem(historyKey,JSON.stringify(conversationHistory));showAnswer(data)}}catch(error){if(error.name!=='AbortError'&&sequence===requestSequence)showAnswer({success:false,route:'error',message:'Request failed: '+error.message})}finally{if(sequence===requestSequence){activeRequest=null;answer.classList.remove('busy');status()}}}
ask.onclick=()=>submit();query.onkeydown=event=>{if(event.key==='Enter')submit()};document.querySelectorAll('[data-q]').forEach(button=>button.onclick=()=>submit(button.dataset.q));
document.getElementById('refresh').onclick=async()=>{try{const data=await jsonResponse(await fetch(apiPath('api/refresh'),{method:'POST'}));answer.textContent=`MCP tools refreshed: ${data.tools}.`;status()}catch(error){answer.textContent='Refresh failed: '+error.message}};
let activeRecognition=null;function startVoice(){stopAudio();if(activeRecognition){activeRecognition.stop();return}const Recognition=window.SpeechRecognition||window.webkitSpeechRecognition;if(!Recognition){answer.textContent='Speech recognition is unavailable in this browser.';return}const recognition=new Recognition();activeRecognition=recognition;recognition.lang='en-GB';const fab=document.getElementById('micFab');fab.classList.add('listening');fab.textContent='■';recognition.onresult=event=>submit(event.results[0][0].transcript);recognition.onerror=event=>{answer.textContent='Speech recognition error: '+event.error};recognition.onend=()=>{activeRecognition=null;fab.classList.remove('listening');fab.textContent='🎤'};recognition.start()}
document.getElementById('speak').onclick=startVoice;document.getElementById('micFab').onclick=startVoice;
status();setInterval(status,30000);
</script></body></html>"""
        .replace("__SAFE_TITLE__", safe_title)
        .replace("__TITLE__", title_json)
        .replace("__VERSION__", version_json)
    )


__all__ = ["render_page"]
