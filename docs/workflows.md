# GitHub workflows

## Validate HomeBrain OS

Runs on every push and pull request. Checks required files and Python syntax.

## Package Hubitat MCP AI add-on

Runs manually or on version tags. Produces `hubitat-mcp-ai-addon.tar.gz` as a workflow artifact.
