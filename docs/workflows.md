# GitHub workflows

## Validate HomeBrain OS

Runs on every push and pull request. Checks required files and Python syntax.

## Package HomeBrain OS add-on

Runs manually or on version tags. Produces `homebrainos-addon.tar.gz` as a workflow artifact.
