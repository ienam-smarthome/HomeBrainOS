# Hubitat MCP AI 0.4.38

This release changes the Home Assistant lifecycle stage from `experimental` to `stable` and keeps the add-on version in plain numeric form.

When Home Assistant shows current release notes but the update dialog still reports an older identical installed/latest version, reload Supervisor metadata with `ha supervisor reload`, then use App store → Check for updates.
